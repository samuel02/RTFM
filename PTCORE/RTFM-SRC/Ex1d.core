Reset {
	#>printf("User reset\n");<#
	async prio 3 task (75*2);
	async prio 2 task (75);	

	#> 
	while (1) {
		// RT_SLEEP(SEC(1)); // (watch you CPU monitor)
	}
	<#	
}

Task task (int i) {
	#>printf("task %d\n", i);<#
}
